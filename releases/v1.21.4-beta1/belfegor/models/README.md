# Local GGUF models

Place the advisor GGUF model here.

Default path:

```text
.minecraft/belfegor/models/lfm2.5-thinking.gguf
```

Set `llmLlamaModelPath` in `belfegor_settings.json` to use a different model file.
